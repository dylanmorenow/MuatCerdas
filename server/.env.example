# Salin ke .env. SQLite dev (file relatif ke prisma/).
DATABASE_URL="file:./dev.db"
PORT=3001

# Auth tipis (opsional) — default NONAKTIF. Set AUTH_ENABLED=true untuk mewajibkan login.
AUTH_ENABLED=false
AUTH_USERNAME=kpp
AUTH_PASSWORD=muatcerdas
AUTH_SECRET=ubah-rahasia-ini-di-produksi
